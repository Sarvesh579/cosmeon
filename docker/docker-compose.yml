services:
  orbit1:
    build:
      context: ..
      dockerfile: docker/node.Dockerfile
    environment:
      NODE_ID: ORBIT-1
      DATA_DIR: /data
    ports:
      - "4001:4000"
    volumes:
      - orbit1:/data

  orbit2:
    build:
      context: ..
      dockerfile: docker/node.Dockerfile
    environment:
      NODE_ID: ORBIT-2
      DATA_DIR: /data
    ports:
      - "4002:4000"
    volumes:
      - orbit2:/data

  orbit3:
    build:
      context: ..
      dockerfile: docker/node.Dockerfile
    environment:
      NODE_ID: ORBIT-3
      DATA_DIR: /data
    ports:
      - "4003:4000"
    volumes:
      - orbit3:/data

  orbit4:
    build:
      context: ..
      dockerfile: docker/node.Dockerfile
    environment:
      NODE_ID: ORBIT-4
      DATA_DIR: /data
    ports:
      - "4004:4000"
    volumes:
      - orbit4:/data

  orbit5:
    build:
      context: ..
      dockerfile: docker/node.Dockerfile
    environment:
      NODE_ID: ORBIT-5
      DATA_DIR: /data
    ports:
      - "4005:4000"
    volumes:
      - orbit5:/data

  orbit6:
    build:
      context: ..
      dockerfile: docker/node.Dockerfile
    environment:
      NODE_ID: ORBIT-6
      DATA_DIR: /data
    ports:
      - "4006:4000"
    volumes:
      - orbit6:/data

  orbit7:
    build:
      context: ..
      dockerfile: docker/node.Dockerfile
    environment:
      NODE_ID: ORBIT-7
      DATA_DIR: /data
    ports:
      - "4007:4000"
    volumes:
      - orbit7:/data

volumes:
  orbit1:
  orbit2:
  orbit3:
  orbit4:
  orbit5:
  orbit6:
  orbit7: